package replete.ui.sdplus.events;

/**
 * A listener that is notified each time a scale's
 * visualization type changes.
 *
 * @author Derek Trumbo
 */

public interface VisTypeChangedListener {
    public void valueChanged(VisTypeChangedEvent e);
}
