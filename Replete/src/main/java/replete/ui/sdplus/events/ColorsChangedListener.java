package replete.ui.sdplus.events;

/**
 * A listener alerted whenever the override colors of a
 * scale panel model change.
 *
 * @author Derek Trumbo
 */

public interface ColorsChangedListener {
    public void valueChanged(ColorsChangedEvent e);
}
