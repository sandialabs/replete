package replete.util;

// Alternate names: DevMain, DebugMain

// Consider changing this to an interface so the
// classes being marked by this class could have
// the flexibility of extending a JFrame, for
// example, which is pretty common for quick
// test/demo apps.

public abstract class DemoMain {

    // This class serves as a marker class for those
    // classes that have main methods AND serve as
    // informal demo/developer apps not meant for
    // end users but which serve as starting points
    // to demo some code in the system to other
    // developers.
    //
    // This class is akin to the "AppMain" class and
    // exists for similar purposes.

}
