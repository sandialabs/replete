package replete.params.hier;

import replete.plugins.SerializableEmptyEqualsObject;

// NOTE: A SINGLE "property" is a Java bean (POJO) that can have
// multiple fields (instance variables).

// This class could have also just been named "Property" as well.
public abstract class PropertyParams extends SerializableEmptyEqualsObject {

    // No methods currently needed (hashCode/equals handled by base classes)

}
