package replete.scrutinize.archive.app2;

public enum ValueType {
    ABSOLUTE_TIME,     // units = ms | ns
    BOOLEAN,
    COUNT,
    DEFAULT,
    ELAPSED_TIME,      // units = ms | ns
    KEY_MASK,
    KEY_VALUE_MAP,
    MEMORY_SIZE,       // units = B (always)
    NAME,
    PATH,
    PATH_LIST,
    VALUE_LIST,
    VERSION,
}
