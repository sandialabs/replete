package finio.ui.view;

import finio.core.KeyValue;

public class SelectionContextSegment extends KeyValue {
    public SelectionContextSegment(Object K, Object V) {
        super(K, V);
    }
}
